<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |e| %>
    $('.answer_errors').html('<%= j e %>');
  <% end %>
<% else %> 
  $('.answer-<%= @answer.id %>').replaceWith("<%= j render @answer %>"); 
<% end %>  
