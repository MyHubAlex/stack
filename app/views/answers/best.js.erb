<% if @answer.errors.present? %>
  <% @question.errors.full_messages.each do |e| %>
    
  <% end %>
<% else %>
  $('.best_answer').removeClass('best_answer'); 
  elem = $('.answer-<%= @answer.id %>'); 
  $('.answers').prepend(elem);    
  $(elem).addClass('best_answer');
<% end %>  